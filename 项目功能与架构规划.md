# 土豆Todo — 项目功能与架构规划

> 基于调研总结报告的技术落地规划  
> 框架：Tauri v2 + React 19 + TypeScript + Rust + SQLite

---

## 一、功能架构总览

```
土豆Todo
├── Phase 1 — MVP 看板核心（当前构建）
│   ├── 项目管理
│   ├── 看板管理
│   ├── 列管理
│   ├── 卡片 CRUD
│   ├── 拖拽排序
│   ├── 子任务
│   ├── 标签系统
│   ├── FTS5 全文搜索
│   ├── 本地 SQLite 存储
│   ├── 基础 UI + 深浅色主题
│   ├── 键盘快捷键
│   └── 数据导入导出
│
├── Phase 2 — 增强功能
│   ├── 表格视图
│   ├── 统一看板
│   ├── 仪表盘
│   ├── 日历/时间线视图
│   ├── WIP 限制
│   ├── 看板模板
│   ├── 自动备份
│   ├── 卡片附件
│   └── 本地网络分享
│
└── Phase 3 — 云同步 & 移动端
    ├── 可选云同步（E2E 加密）
    ├── Tauri Mobile（iOS/Android）
    ├── 移动端触屏优化
    └── 应用商店上架
```

---

## 二、项目目录结构

```
todo-potato/
├── src-tauri/                    # Rust 后端
│   ├── src/
│   │   ├── main.rs               # Tauri 入口
│   │   ├── lib.rs                 # 模块导出
│   │   ├── db/
│   │   │   ├── mod.rs             # 数据库模块入口
│   │   │   ├── migrations.rs      # 数据库迁移（schema 版本管理）
│   │   │   ├── projects.rs        # 项目 CRUD
│   │   │   ├── boards.rs          # 看板 CRUD
│   │   │   ├── columns.rs         # 列 CRUD + 排序
│   │   │   ├── cards.rs           # 卡片 CRUD + 拖拽排序
│   │   │   ├── subtasks.rs        # 子任务 CRUD
│   │   │   ├── tags.rs            # 标签 CRUD
│   │   │   └── search.rs          # FTS5 全文搜索
│   │   ├── commands/
│   │   │   ├── mod.rs             # Tauri commands 入口
│   │   │   ├── project_cmds.rs    # 项目相关命令
│   │   │   ├── board_cmds.rs      # 看板相关命令
│   │   │   ├── column_cmds.rs     # 列相关命令
│   │   │   ├── card_cmds.rs       # 卡片相关命令
│   │   │   ├── subtask_cmds.rs    # 子任务相关命令
│   │   │   ├── tag_cmds.rs        # 标签相关命令
│   │   │   ├── search_cmds.rs     # 搜索命令
│   │   │   └── settings_cmds.rs   # 设置命令
│   │   ├── models/
│   │   │   ├── mod.rs             # 数据模型入口
│   │   │   ├── project.rs         # Project 结构体
│   │   │   ├── board.rs           # Board 结构体
│   │   │   ├── column.rs          # Column 结构体
│   │   │   ├── card.rs            # Card 结构体
│   │   │   ├── subtask.rs         # Subtask 结构体
│   │   │   ├── tag.rs             # Tag 结构体
│   │   │   └── search.rs          # 搜索结果结构体
│   │   └── backup.rs              # 自动备份逻辑
│   ├── migrations/                # SQL 迁移文件
│   │   └── 001_initial.sql
│   ├── Cargo.toml
│   └── tauri.conf.json
│
├── src/                           # React 前端
│   ├── main.tsx                   # React 入口
│   ├── App.tsx                    # 根组件（路由 + 全局布局）
│   ├── routes/
│   │   ├── index.tsx              # 路由定义
│   │   ├── ProjectPage.tsx        # 项目详情页
│   │   ├── BoardPage.tsx          # 看板主页
│   │   └── SearchPage.tsx         # 搜索结果页
│   ├── components/
│   │   ├── layout/
│   │   │   ├── AppShell.tsx        # 整体布局框架
│   │   │   ├── Sidebar.tsx         # 左侧导航栏
│   │   │   ├── Topbar.tsx          # 顶部标题栏
│   │   │   └── SearchBar.tsx       # 全局搜索框
│   │   ├── board/
│   │   │   ├── BoardView.tsx       # 看板视图主体
│   │   │   ├── Column.tsx          # 单个列
│   │   │   ├── Card.tsx            # 单个卡片
│   │   │   ├── CardDialog.tsx      # 卡片详情弹窗
│   │   │   ├── CardForm.tsx        # 卡片编辑表单
│   │   │   ├── ColumnHeader.tsx    # 列头部
│   │   │   ├── AddColumnButton.tsx # 添加列按钮
│   │   │   └── AddCardButton.tsx   # 添加卡片按钮
│   │   ├── project/
│   │   │   ├── ProjectList.tsx     # 项目列表
│   │   │   ├── ProjectCard.tsx     # 项目卡片
│   │   │   └── ProjectDialog.tsx   # 项目编辑弹窗
│   │   ├── tags/
│   │   │   ├── TagBadge.tsx        # 标签徽标
│   │   │   ├── TagSelector.tsx     # 标签选择器
│   │   │   └── TagManager.tsx      # 标签管理弹窗
│   │   ├── subtask/
│   │   │   └── SubtaskList.tsx     # 子任务列表
│   │   ├── search/
│   │   │   └── SearchResults.tsx   # 搜索结果展示
│   │   └── ui/                     # shadcn/ui 组件
│   │       ├── Button.tsx
│   │       ├── Dialog.tsx
│   │       ├── Input.tsx
│   │       ├── Select.tsx
│   │       ├── DropdownMenu.tsx
│   │       ├── Tooltip.tsx
│   │       ├── Badge.tsx
│   │       └── ...
│   ├── hooks/
│   │   ├── useProjects.ts          # 项目数据 hooks
│   │   ├── useBoard.ts             # 看板数据 hooks
│   │   ├── useCards.ts             # 卡片数据 hooks
│   │   ├── useColumns.ts           # 列数据 hooks
│   │   ├── useTags.ts              # 标签数据 hooks
│   │   ├── useSearch.ts            # 搜索 hooks
│   │   └── useSettings.ts          # 设置 hooks
│   ├── stores/
│   │   ├── appStore.ts             # 全局 UI 状态
│   │   ├── boardStore.ts           # 当前看板状态
│   │   └── dragStore.ts            # 拖拽过程中的临时状态
│   ├── lib/
│   │   ├── tauri-api.ts            # Tauri invoke 封装
│   │   ├── utils.ts                # 通用工具函数
│   │   └── constants.ts            # 常量定义
│   ├── types/
│   │   ├── project.ts
│   │   ├── board.ts
│   │   ├── column.ts
│   │   ├── card.ts
│   │   ├── subtask.ts
│   │   ├── tag.ts
│   │   └── common.ts              # 通用类型
│   └── styles/
│       └── globals.css             # Tailwind 入口
│
├── index.html
├── package.json
├── tsconfig.json
├── tailwind.config.ts
├── vite.config.ts
├── pnpm-lock.yaml
└── README.md
```

---

## 三、Tauri 命令（Rust → 前端 API）设计

所有前后端通信通过 Tauri `invoke` 命令完成。

### 3.1 项目管理

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_project` | name, description?, color? | Project | 创建项目 |
| `get_all_projects` | — | Vec\<Project\> | 获取所有项目 |
| `get_project` | id: String | Project | 获取单个项目 |
| `update_project` | id, name?, description?, color? | Project | 更新项目 |
| `delete_project` | id: String | () | 删除项目（级联删除其下所有数据） |

### 3.2 看板管理

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_board` | project_id, name | Board | 创建看板 |
| `get_boards_by_project` | project_id | Vec\<Board\> | 获取项目下所有看板 |
| `update_board` | id, name? | Board | 更新看板 |
| `delete_board` | id | () | 删除看板（级联删除列和卡片） |
| `reorder_boards` | ids: Vec\<String\> (ordered) | () | 拖拽看板重新排序 |

### 3.3 列管理

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_column` | board_id, name, sort_order? | Column | 创建列 |
| `get_columns_by_board` | board_id | Vec\<Column\> | 获取看板下所有列 |
| `update_column` | id, name?, wip_limit? | Column | 更新列 |
| `delete_column` | id | () | 删除列 |
| `reorder_columns` | ids: Vec\<String\> (ordered) | () | 拖拽列重新排序 |

### 3.4 卡片管理

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_card` | column_id, title | Card | 创建卡片 |
| `get_cards_by_column` | column_id | Vec\<Card\> | 获取列下所有卡片 |
| `get_card` | id | Card (with subtasks & tags) | 获取卡片完整信息 |
| `update_card` | id, title?, description?, due_date?, priority? | Card | 更新卡片 |
| `move_card` | card_id, target_column_id, target_sort_order | () | 拖拽移动卡片到目标列 |
| `move_card_within_column` | card_id, target_sort_order | () | 同列内拖拽排序 |
| `archive_card` | id | () | 归档卡片 |
| `delete_card` | id | () | 永久删除卡片 |

### 3.5 子任务

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_subtask` | card_id, title | Subtask | 创建子任务 |
| `toggle_subtask` | id | Subtask | 切换子任务完成状态 |
| `delete_subtask` | id | () | 删除子任务 |

### 3.6 标签

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `create_tag` | board_id, name, color | Tag | 创建标签 |
| `get_tags_by_board` | board_id | Vec\<Tag\> | 获取看板下所有标签 |
| `delete_tag` | id | () | 删除标签 |
| `add_tag_to_card` | card_id, tag_id | () | 给卡片添加标签 |
| `remove_tag_from_card` | card_id, tag_id | () | 从卡片移除标签 |
| `get_tags_by_card` | card_id | Vec\<Tag\> | 获取卡片上的所有标签 |

### 3.7 搜索

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `search_cards` | query: String, project_id? | Vec\<SearchResult\> | FTS5 全文搜索 |

### 3.8 设置与工具

| 命令 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `get_setting` | key: String | String? | 获取设置 |
| `set_setting` | key, value | () | 设置配置项 |
| `export_data` | path: String | () | 导出所有数据为 JSON |
| `import_data` | path: String | () | 从 JSON 导入数据 |
| `get_db_stats` | — | DbStats | 获取数据库统计信息 |

---

## 四、前端路由设计

使用 React Router v7：

| 路由 | 页面组件 | 说明 |
|------|----------|------|
| `/` | 重定向到首次跳转 | 空状态引导页 |
| `/projects` | ProjectListPage | 项目列表主页 |
| `/project/:projectId` | ProjectPage | 项目详情（显示看板列表） |
| `/board/:boardId` | BoardPage | 看板主页（核心页面） |
| `/search` | SearchPage | 全局搜索结果页 |

### 导航逻辑

```
AppShell
├── Sidebar (左侧)
│   ├── 项目列表（点击切换项目）
│   └── 项目下看板列表（点击切看板）
├── 主内容区 (根据路由渲染)
│   ├── /projects          → ProjectListPage
│   ├── /project/:id       → ProjectPage
│   ├── /board/:id         → BoardPage
│   └── /search            → SearchPage
└── Topbar (顶部)
    ├── 当前页面标题
    ├── 搜索框（全局 Cmd+K）
    └── 设置/主题切换按钮
```

---

## 五、前端组件层级关系

```
App
└── AppShell
    ├── Sidebar
    │   ├── Logo
    │   ├── SearchBar (快速搜索)
    │   ├── ProjectList
    │   │   └── ProjectCard[] (每个项目)
    │   │       └── BoardItem[] (项目下的看板列表)
    │   ├── AddProjectButton
    │   └── SettingsButton
    │
    ├── Topbar
    │   ├── Breadcrumb (项目 / 看板)
    │   ├── BoardTabs (看板切换 tab)
    │   ├── SearchTrigger (Cmd+K)
    │   └── ThemeToggle
    │
    └── MainContent
        ├── [路由: /projects]
        │   └── ProjectListPage
        │       ├── ProjectGrid (卡片网格)
        │       │   └── ProjectCard[] (项目卡片)
        │       └── CreateProjectDialog
        │
        ├── [路由: /project/:id]
        │   └── ProjectPage
        │       ├── ProjectHeader (项目名称 + 操作按钮)
        │       └── BoardList
        │           ├── BoardCard[] (看板卡片)
        │           └── CreateBoardDialog
        │
        ├── [路由: /board/:id]  ← 核心页面
        │   └── BoardPage
        │       ├── BoardHeader (看板名称 + 标签管理)
        │       └── BoardView (滚动容器)
        │           ├── Column[] (无限水平滚动)
        │           │   ├── ColumnHeader
        │           │   │   ├── ColumnName (可编辑)
        │           │   │   ├── CardCount
        │           │   │   ├── WipIndicator (Phase 2)
        │           │   │   └── ColumnMenu (编辑/删除/排序)
        │           │   ├── Card[] (拖拽容器)
        │           │   │   ├── CardCover (颜色标记)
        │           │   │   ├── CardTitle
        │           │   │   ├── TagBadge[] (标签)
        │           │   │   ├── PriorityIcon
        │           │   │   ├── DueDate
        │           │   │   ├── SubtaskSummary (已完成/总数)
        │           │   │   └── CardMenu (编辑/归档/删除)
        │           │   └── AddCardButton
        │           └── AddColumnButton
        │
        └── [路由: /search]
            └── SearchPage
                ├── SearchInput
                └── SearchResults
                    └── SearchResultItem[] (卡片标题匹配 + 上下文)

全域弹窗（Overlay）
├── CardDialog (卡片详情)
│   ├── CardTitle (编辑)
│   ├── Description (Tiptap 富文本编辑器)
│   ├── DueDatePicker
│   ├── PrioritySelector
│   ├── TagSelector
│   ├── SubtaskList
│   └── ActivityLog (Phase 2)
├── ProjectDialog (创建/编辑项目)
├── TagManager (管理看板标签)
└── SettingsDialog (应用设置)
```

---

## 六、数据流架构

### 6.1 单向数据流

```
用户操作 (拖拽/点击/输入)
        │
        ▼
React 组件 → Zustand (UI 状态) / React Query (服务端状态)
        │
        ▼
TanStack React Query (缓存层)
        │
        ▼
tauri.invoke("command_name", { args })
        │
        ▼
Tauri Commands (Rust)
        │
        ▼
rusqlite (SQLite 数据库)
        │
        ▼
返回 Result<T, String> 给前端
        │
        ▼
React Query 自动更新缓存 → UI 重新渲染
```

### 6.2 状态管理分工

| 状态类型 | 管理工具 | 示例 |
|----------|----------|------|
| **服务端状态**（数据库数据） | TanStack React Query | 项目列表、看板数据、卡片数据 |
| **UI 状态**（无需持久化） | Zustand | 当前选中项目/看板、侧边栏展开/收起、主题模式 |
| **拖拽状态**（临时） | Zustand（或 Pragmatic DnD 内部状态） | 拖拽中的源/目标位置、动画状态 |
| **表单状态** | React 本地 state / React Hook Form | 卡片编辑弹窗的输入值 |

### 6.3 缓存策略

```
TanStack React Query:
├── queryKey: ["projects"]                    → staleTime: 5min (项目不常变)
├── queryKey: ["boards", projectId]           → staleTime: 5min
├── queryKey: ["columns", boardId]            → staleTime: Infinity (几乎不变)
├── queryKey: ["cards", columnId]             → staleTime: 0 (实时更新，拖拽频繁)
├── queryKey: ["card", cardId]                → staleTime: 0
├── queryKey: ["tags", boardId]               → staleTime: Infinity
└── queryKey: ["search", query]               → staleTime: 0 (搜索结果即时)
```

- **高频操作**（拖拽移动卡片）→ `staleTime: 0`，拖拽完成后乐观更新 + 后端确认
- **低频操作**（项目/看板元数据）→ `staleTime: 5min`，减少不必要查询
- **常量数据**（列结构、标签）→ `staleTime: Infinity`，只在变更时主动失效

---

## 七、数据库设计（SQL）

### 7.1 完整建表 SQL

```sql
-- 开启 WAL 模式
PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;
PRAGMA encoding = 'UTF-8';

CREATE TABLE projects (
    id          TEXT PRIMARY KEY,  -- UUID v4
    name        TEXT NOT NULL,
    description TEXT DEFAULT '',
    color       TEXT DEFAULT '#6366f1',  -- indigo-500
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE boards (
    id          TEXT PRIMARY KEY,
    project_id  TEXT NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    description TEXT DEFAULT '',
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_boards_project ON boards(project_id);

CREATE TABLE columns (
    id          TEXT PRIMARY KEY,
    board_id    TEXT NOT NULL REFERENCES boards(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    wip_limit   INTEGER DEFAULT NULL,  -- Phase 2
    color       TEXT DEFAULT '#6b7280',  -- gray-500
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_columns_board ON columns(board_id);

CREATE TABLE cards (
    id            TEXT PRIMARY KEY,
    column_id     TEXT NOT NULL REFERENCES columns(id) ON DELETE CASCADE,
    title         TEXT NOT NULL,
    description   TEXT DEFAULT '{}',  -- Tiptap JSON 格式
    sort_order    INTEGER NOT NULL DEFAULT 0,
    priority      INTEGER DEFAULT 0,  -- 0: none, 1: low, 2: medium, 3: high
    due_date      TEXT DEFAULT NULL,  -- ISO 8601
    cover_color   TEXT DEFAULT NULL,  -- 封面颜色
    is_archived   INTEGER DEFAULT 0,  -- 0: active, 1: archived
    created_at    TEXT NOT NULL DEFAULT (datetime('now')),
    updated_at    TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_cards_column ON cards(column_id);
CREATE INDEX idx_cards_archived ON cards(is_archived);

CREATE TABLE subtasks (
    id          TEXT PRIMARY KEY,
    card_id     TEXT NOT NULL REFERENCES cards(id) ON DELETE CASCADE,
    title       TEXT NOT NULL,
    is_done     INTEGER DEFAULT 0,
    sort_order  INTEGER NOT NULL DEFAULT 0,
    created_at  TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE INDEX idx_subtasks_card ON subtasks(card_id);

CREATE TABLE tags (
    id        TEXT PRIMARY KEY,
    board_id  TEXT NOT NULL REFERENCES boards(id) ON DELETE CASCADE,
    name      TEXT NOT NULL,
    color     TEXT DEFAULT '#3b82f6',  -- blue-500
    created_at TEXT NOT NULL DEFAULT (datetime('now')),
    UNIQUE(board_id, name)
);
CREATE INDEX idx_tags_board ON tags(board_id);

CREATE TABLE card_tags (
    card_id TEXT NOT NULL REFERENCES cards(id) ON DELETE CASCADE,
    tag_id  TEXT NOT NULL REFERENCES tags(id) ON DELETE CASCADE,
    PRIMARY KEY (card_id, tag_id)
);
CREATE INDEX idx_card_tags_card ON card_tags(card_id);
CREATE INDEX idx_card_tags_tag ON card_tags(tag_id);

CREATE TABLE settings (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL  -- JSON
);

-- FTS5 全文搜索虚拟表
CREATE VIRTUAL TABLE cards_fts USING fts5(
    card_id UNINDEXED,
    title,
    description,
    content='cards',
    content_rowid='rowid',
    tokenize='unicode61'
);

-- 触发器：保持 FTS 索引与 cards 表同步
CREATE TRIGGER cards_ai AFTER INSERT ON cards BEGIN
    INSERT INTO cards_fts(rowid, card_id, title, description)
    VALUES (new.rowid, new.id, new.title, new.description);
END;

CREATE TRIGGER cards_ad AFTER DELETE ON cards BEGIN
    INSERT INTO cards_fts(cards_fts, rowid, card_id, title, description)
    VALUES ('delete', old.rowid, old.id, old.title, old.description);
END;

CREATE TRIGGER cards_au AFTER UPDATE ON cards BEGIN
    INSERT INTO cards_fts(cards_fts, rowid, card_id, title, description)
    VALUES ('delete', old.rowid, old.id, old.title, old.description);
    INSERT INTO cards_fts(rowid, card_id, title, description)
    VALUES (new.rowid, new.id, new.title, new.description);
END;
```

---

## 八、Tauri 配置要点

```toml
# src-tauri/Cargo.toml 核心依赖
[dependencies]
tauri = { version = "2", features = ["dialog", "shell", "notification"] }
tauri-plugin-dialog = "2"
tauri-plugin-shell = "2"
tauri-plugin-notification = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rusqlite = { version = "0.32", features = ["bundled", "fts5"] }
uuid = { version = "1", features = ["v4"] }
chrono = "0.4"
tokio = { version = "1", features = ["full"] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
```

```json
// src-tauri/tauri.conf.json (关键配置)
{
  "identifier": "com.todopotato.app",
  "build": {
    "frontendDist": "../dist",
    "devUrl": "http://localhost:5173",
    "beforeDevCommand": "pnpm dev",
    "beforeBuildCommand": "pnpm build"
  },
  "bundle": {
    "active": true,
    "icon": ["icons/icon.png"],
    "targets": ["msi", "dmg", "appimage"],
    "windows": {
      "wix": {
        "language": "zh-CN"
      }
    }
  },
  "app": {
    "windows": [
      {
        "title": "土豆Todo",
        "width": 1200,
        "height": 800,
        "minWidth": 800,
        "minHeight": 600,
        "resizable": true,
        "fullscreen": false
      }
    ]
  },
  "plugins": {
    "dialog": {},
    "shell": {
      "open": true
    }
  }
}
```

---

## 九、开发阶段与执行顺序

### 第一步：项目初始化（Day 1）

1. `pnpm create tauri-app todo-potato --template react-ts`
2. 安装前端依赖：`tailwind v4`, `shadcn/ui`, `zustand`, `@tanstack/react-query`, `@atlaskit/pragmatic-drag-and-drop`, `@tiptap/react`, `react-router`
3. 配置 Rust 依赖：`rusqlite` (bundled + fts5), `uuid`, `serde`, `chrono`
4. 清空模板代码，建立目录结构

### 第二步：数据库层（Day 2-3）

1. 实现 `migrations.rs` — 建表 + FTS5
2. 实现 `models/` — 所有 Rust 结构体，derive Serialize/Deserialize
3. 实现 `db/projects.rs`, `boards.rs`, `columns.rs` — 基础 CRUD
4. 单元测试数据库操作

### 第三步：Tauri 命令层（Day 4-5）

1. 实现 `commands/` — 将所有 db 操作包装为 Tauri 命令
2. 前端 `lib/tauri-api.ts` — invoke 封装 + 类型安全

### 第四步：前端基础框架（Day 6-8）

1. 实现 AppShell + Sidebar + Topbar 布局
2. 实现路由系统
3. 实现 TanStack React Query hooks
4. 实现 Zustand stores

### 第五步：看板核心 UI（Day 9-14）

1. 实现 BoardView + Column + Card 组件
2. 集成 Pragmatic DnD（列间拖拽 + 列内排序）
3. 实现 CardDialog（Tiptap 富文本编辑器）
4. 实现搜索功能

### 第六步：辅助功能（Day 15-17）

1. 实现子任务
2. 实现标签系统
3. 实现深浅色主题
4. 实现键盘快捷键
5. 实现数据导入导出
6. 打包 MSI / DMG / AppImage

---

## 十、关键设计决策说明

### 10.1 为什么用 React Query 而非直接调用 Tauri 命令？

虽然 Tauri invoke 本质是异步函数，但 React Query 提供了：
- 自动缓存 + 去重（避免同一数据多次查询）
- 乐观更新（拖拽卡片后先更新 UI，再等待后端确认）
- 错误重试 + 回滚（网络/数据库错误时 UI 不崩溃）
- 数据失效 + 自动刷新（某个命令成功后，相关 query 自动刷新）

### 10.2 为什么用 Zustand 而非 Context/Redux？

- Zustand 包体积 ~1KB（Redux Toolkit ~11KB）
- 无需 Provider 包裹
- 可在组件外部使用（方便拖拽回调中访问状态）
- TypeScript 友好
- Zustand v5 支持 Signals，性能更优

### 10.3 拖拽数据持久化策略

```
用户拖拽卡片到新列
        │
        ▼
Pragmatic DnD 触发 onDrop 回调
        │
        ▼
1. 乐观更新：React Query cache 中立即更新卡片位置
2. UI 即时反馈（无等待）
        │
        ▼
3. 并行调用：tauri.invoke("move_card", { ... })
        │
        ▼
4. 成功 → React Query 刷新确认数据
5. 失败 → 回滚到之前的位置 + 错误提示
```

### 10.4 Tiptap 富文本存储格式

```json
{
  "type": "doc",
  "content": [
    {
      "type": "paragraph",
      "content": [
        { "type": "text", "text": "这是普通文本 " },
        { "type": "text", "marks": [{ "type": "bold" }], "text": "加粗" },
        { "type": "text", "text": " " },
        { "type": "text", "marks": [{ "type": "link", "attrs": { "href": "https://..." } }], "text": "链接" }
      ]
    },
    {
      "type": "taskList",
      "content": [
        { "type": "taskItem", "attrs": { "checked": false }, "content": [{ "type": "paragraph", "content": [{ "type": "text", "text": "待办项" }] }] }
      ]
    },
    { "type": "codeBlock", "attrs": { "language": "typescript" }, "content": [{ "type": "text", "text": "const x = 1;" }] }
  ]
}
```

### 10.5 自动备份策略

```
自动备份（后台线程）
├── 触发时机：
│   ├── 应用启动
│   ├── 每次卡片移动成功后
│   └── 每 5 分钟定时
├── 备份文件格式：
│   ├── 完整 .sqlite 副本（压缩）
│   └── JSON 增量导出（轻量文本备份）
├── 保留策略：
│   ├── 最近 3 个全量备份
│   └── 最近 30 个增量备份
└── 存储路径：
    └── ~/TodoPotato/backups/
```

---

> **文档版本：** v1.0  
> **最后更新：** 2026-05-25  
> **下一步：** 确认规划后开始项目初始化
