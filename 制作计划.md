# Todo土豆 — 制作计划

## Phase 1 MVP

### ✅ 已完成

| # | 任务 | 说明 |
|---|------|------|
| 1.1 | 项目初始化 | Tauri + React + Vite + Tailwind + pnpm 框架搭建 |
| 1.2 | 数据库层 | migrations + Rust models + db CRUD + FTS5 |
| 1.3 | Tauri 命令层 | 所有 commands + 前端 `tauri-api.ts` 绑定 |
| 1.4 | 项目管理 | `ProjectList.tsx` 列表/创建/编辑/删除 |
| 1.5 | 看板管理 | `ProjectDetail.tsx` 看板列表/创建/编辑/删除 |
| 1.6 | 列管理 | `BoardView.tsx` 列创建/删除（缺列编辑菜单） |
| 1.7 | 卡片 CRUD | `BoardView.tsx` 添加/编辑/删除/归档/跨列拖拽 |
| 1.8 | 子任务 | `CardDetailDialog` 创建/勾选/删除 |
| 1.9 | 标签系统 | `TagBadge` / `TagSelector` / `TagManager` 全套 |
| 1.10 | FTS5 全文搜索 | `SearchPage.tsx` 搜索展示 |
| 1.11 | 深浅色主题 | `Topbar.tsx` 切换 + CSS 变量 |
| 1.12 | 布局框架 | `AppShell` / `Sidebar` / `Topbar` 面包屑 |

### ❌ 待完成（按优先级排列）

| # | 任务 | 工作量 | 依赖 |
|---|------|--------|------|
| **2.1** | **Pragmatic DnD 替换原生拖拽** | 中 | ✅ 已完成 — @atlaskit/pragmatic-drag-and-drop + auto-scroll |
| **2.2** | **TipTap 富文本编辑器** | 中 | ✅ 已完成 — @tiptap/react + StarterKit + Link + TaskList |
| **2.3** | **提取 React Query hooks** | 小 | ✅ 已完成 — `useProjects` / `useBoard` / `useCards` / `useColumns` / `useTags` / `useSubtasks` / `useSearch` + 迁移所有路由文件 |
| **2.4** | **补齐 Zustand stores** | 小 | ✅ 已完成 — `boardStore.ts`（卡片详情/Tag管理器状态）+ `dragStore.ts`（全局拖拽跟踪）|
| **2.5** | **列上下文菜单** | 小 | ✅ 已完成 — ColumnView 添加编辑列名（行内输入）/删除列 DropdownMenu |
| **2.6** | **看板头部显示看板名称** | 极小 | ✅ 已完成 — 新增 `get_board` Rust 命令 + `useBoardById` hook，BoardView 标题改为显示 `board.name` |
| **2.7** | **键盘快捷键** | 中 | ✅ 已完成 — `useKeyboardShortcuts` hook + `Mod+K` 全局搜索 / `N` 新建列 / `Escape` 关闭卡片详情 |
| **2.8** | **数据导入导出** | 大 | ✅ 已完成 — Rust `export_data` / `import_data` 命令（JSON 文件）+ `get_all` 批量查询 + 侧栏底部导入导出按钮 |

---

## Phase 2（增强功能）

- 表格视图
- 统一看板
- 仪表盘
- 日历/时间线视图
- WIP 限制（数据库字段已预留）
- 看板模板
- 自动备份
- 卡片附件

## Phase 3（云同步 & 移动端）

- 可选云同步（E2E 加密）
- Tauri Mobile（iOS/Android）
- 移动端触屏优化
- 应用商店上架
