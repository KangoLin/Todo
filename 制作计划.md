# Todo土豆 — 制作计划

## Phase 1 MVP — ✅ 已完成

| # | 任务 | 说明 |
|---|------|------|
| 1.1 | 项目初始化 | Tauri + React + Vite + Tailwind + pnpm 框架搭建 |
| 1.2 | 数据库层 | migrations + Rust models + db CRUD + FTS5 |
| 1.3 | Tauri 命令层 | 所有 commands + 前端 `tauri-api.ts` 绑定 |
| 1.4 | 项目管理 | `ProjectList.tsx` 列表/创建/编辑/删除 |
| 1.5 | 看板管理 | `ProjectDetail.tsx` 看板列表/创建/编辑/删除 |
| 1.6 | 列管理 + 列上下文菜单 | `BoardView.tsx` 列创建/编辑/删除 |
| 1.7 | 卡片 CRUD | `BoardView.tsx` 添加/编辑/删除/归档/跨列拖拽 |
| 1.8 | 子任务 | `CardDetailDialog` 创建/勾选/删除 |
| 1.9 | 标签系统 | `TagBadge` / `TagSelector` / `TagManager` 全套 |
| 1.10 | FTS5 全文搜索 | `SearchPage.tsx` 搜索展示 |
| 1.11 | 深浅色主题 | `Topbar.tsx` 切换 + CSS 变量 |
| 1.12 | 布局框架 | `AppShell` / `Sidebar` / `Topbar` 面包屑 |
| 2.1 | Pragmatic DnD 替换原生拖拽 | @atlaskit/pragmatic-drag-and-drop + auto-scroll |
| 2.2 | TipTap 富文本编辑器 | @tiptap/react + StarterKit + Link + TaskList |
| 2.3 | 提取 React Query hooks | `useProjects` / `useBoard` / `useCards` / `useColumns` / `useTags` / `useSubtasks` / `useSearch` |
| 2.4 | 补齐 Zustand stores | `boardStore` / `dragStore` |
| 2.5 | 列上下文菜单 | 编辑列名 + 删除列 |
| 2.6 | 看板头部显示看板名称 | `get_board` 命令 + `useBoardById` |
| 2.7 | 键盘快捷键 | `Mod+K` 搜索 / `N` 新建列 / `Escape` 关闭 |
| 2.8 | 数据导入导出 | Rust JSON 导入导出 + 侧栏文件选择器 |
| 2.9 | 列拖拽排序 | Pragmatic DnD 列拖动重排 + `reorderColumns` 命令 |
| 2.10 | 列内卡片拖拽排序 | 同列卡片重排 via `moveCardWithinColumn` |
| 2.11 | 归档面板 | 右侧滑出面板 + `restore_card`/`get_archived_cards_by_board` 命令 |
| 2.12 | UI 精细打磨 | Trello 风格卡片阴影/悬停效果、列拖拽指示器、空状态占位图 |

---

## Phase 2（增强功能）

- 表格视图
- 统一看板
- 仪表盘
- 日历/时间线视图
- WIP 限制（数据库字段已预留）
- 看板模板
- 自动备份
- 卡片附件

## Phase 3（云同步 & 移动端）

- 可选云同步（E2E 加密）
- Tauri Mobile（iOS/Android）
- 移动端触屏优化
- 应用商店上架
